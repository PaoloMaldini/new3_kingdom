spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://127.0.0.1:3306/wangquan?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:change_me}
    driver-class-name: com.mysql.cj.jdbc.Driver

app:
  cors:
    allowed-origins:
      - ${APP_CORS_ALLOWED_ORIGIN:https://your-domain.com}
